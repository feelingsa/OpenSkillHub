# Workflow Console Preset v1

Prioritize the declared workflow and long-running execution. Present workflow steps as a clear vertical sequence with a focused task input area. The runtime status, event feed, question/permission region, and artifacts should be prominent and readable without leaving the page. Do not fabricate step controls for workflow entries that the manifest does not define.
