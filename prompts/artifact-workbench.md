# Artifact Workbench Preset v1

Prioritize declared outputs and result inspection while keeping required inputs compact. Reserve the largest stable panel for the shared runtime artifact mount, with runtime status and event history nearby. Do not invent preview tooling; the shared runtime provides secure preview and download behavior.
